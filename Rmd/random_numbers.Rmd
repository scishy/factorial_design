---
title: "random numbers"
author: "Shylo Burrell"
date: "1/20/2021"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}

#runif(n) -- generates n numbers between 0 and 1 all with equal probability
#rnorm(n) -- generates n numbers from N(0,1)
#rpois(n, 5) -- generates n counts with mean 5

#runif(n, min = 0, max = 1)

runif(1, min = 1, max = 45)

```

```{r}
runif(1, min = 1, max = 7)
```

```{r}
#use to choose plots- two from each paper

runif(2, min = 1, max = 7)

```

